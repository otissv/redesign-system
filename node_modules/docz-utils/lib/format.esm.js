import { format as format$1 } from 'prettier';
import logger from 'signale';

const formatter = (code) => format$1(code, {
    parser: 'babel',
    semi: false,
    singleQuote: true,
    trailingComma: 'all',
});
const format = (code) => new Promise((resolve, reject) => {
    try {
        const result = formatter(code);
        resolve(result);
    }
    catch (err) {
        logger.fatal(err);
        resolve(code);
    }
});

export { format, formatter };
