'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var prettier = require('prettier');
var logger = _interopDefault(require('signale'));

const formatter = (code) => prettier.format(code, {
    parser: 'babel',
    semi: false,
    singleQuote: true,
    trailingComma: 'all',
});
const format = (code) => new Promise((resolve, reject) => {
    try {
        const result = formatter(code);
        resolve(result);
    }
    catch (err) {
        logger.fatal(err);
        resolve(code);
    }
});

exports.format = format;
exports.formatter = formatter;
