import * as React from 'react';
import { MenuItem } from '../state';
declare type Menus = MenuItem[];
export declare type MenuRenderProps = Menus;
export interface DocsProps {
    children?: (renderProps: MenuRenderProps) => React.ReactNode;
}
export declare const Menu: React.SFC<DocsProps>;
export {};
