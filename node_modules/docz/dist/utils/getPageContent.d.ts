import * as React from 'react';
import { ComponentType } from 'react';
export interface Page {
    title: string;
    route: string;
    import: () => Promise<{
        default: ComponentType<any>;
        getInitialData?: () => any;
    }>;
}
export declare function getPageContent(page: Page): Promise<React.ReactNode>;
