/// <reference types="react" />
import { CardHeaderInterface } from './card.types';
declare function CardHeader({ children, className, themed, ...propsRest }: CardHeaderInterface): JSX.Element;
declare namespace CardHeader {
    var defaultProps: {
        className: string;
        themed: any[];
    };
}
export default CardHeader;
export default CardHeader;
