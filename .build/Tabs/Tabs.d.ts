/// <reference types="react" />
import { TabsInterface } from './tabs.types';
declare function Tabs({ className, children, appearance, stretch, size, themed, height, width, stacked, ...propsRest }: TabsInterface): JSX.Element;
declare namespace Tabs {
    var defaultProps: {
        className: string;
        themed: any[];
    };
}
export default Tabs;
export default Tabs;
