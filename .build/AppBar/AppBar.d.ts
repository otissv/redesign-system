/// <reference types="react" />
import { AppBarInterface } from './appBar.types';
declare function AppBar({ children, className, themed, ...propsRest }: AppBarInterface): JSX.Element;
declare namespace AppBar {
    var defaultProps: {
        className: string;
        themed: any[];
    };
}
export default AppBar;
export default AppBar;
