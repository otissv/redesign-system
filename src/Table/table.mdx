---
name: Table
route: /table
---

import { Playground, Props } from 'docz'
import { Flex } from '../Flex/Flex'
import styled from 'styled-components'
import { ThemeProvider } from '../ThemeContext/ThemeProvider'
import { Table } from './Table'
import { useTable } from './table.example'
import { TableRowSelect } from './TableRowSelect'
import { AllSelectedHeading } from './AllSelectedHeading'
import { TableColumn } from './TableColumn'
import { TableRowDetail } from './TableRowDetail'

### Render Props Detail

<Playground>
  <ThemeProvider>
    <Table
      height='250px'
      name='TableRowDetailRenderProps'
      caption="Table caption"
      baseRoute='/table/#'
      hook={useTable}
    >

    <TableColumn
          heading='#'
          uid='index'
          prop='__index'
        />

      <TableColumn
          heading='ID'
          uid='id'
          prop='id'
        />

      <TableColumn
        heading='Name'
        uid='name'
        prop='name'
      />

      <TableColumn
        heading='Endpoint'
        uid='endpoint'

      >
        {({ data }) => <td>
          <a href='/table/#' onClick={e => e.preventDefault()}>{data.endpoint}</a>
        </td>}

      </TableColumn>

      <TableRowDetail uid='detail'>
        {({ data }) => <table>
          <thead>
            <tr>
              <th>IP</th>
              <th>Repository</th>
              <th>Port</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>{data.ip}</td>
              <td>{data.repo}</td>
              <td>{data.port}</td>
            </tr>
          </tbody>
          </table>}
      </TableRowDetail>
    </Table>

  </ThemeProvider>
</Playground>
