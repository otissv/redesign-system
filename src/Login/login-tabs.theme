export function loginTabsTheme({
  theme: {
    animation,
    unit: { px },
  },
}) {
  return {
    display: 'flex',
    flex: 1,
    width: '100%',
    marginBottom: `${px.medium}`,
    transition: animation.easeMedium(),
  }
}
